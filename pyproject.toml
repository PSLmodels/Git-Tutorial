[project]
name = "git-tutorial-book"
version = "0.1.1"
description = "Build configuration for the PSLmodels Git Tutorial book."
requires-python = ">=3.11"
dependencies = [
    "jupyter-book>=2.0.0,<3",
]

[tool.uv]
package = false
